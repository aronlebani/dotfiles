# ~/.bash_login: executed by the command interpreter for login shells.

if [ -n "$BASH_VERSION" ]; then
    if [ -f "$HOME/.bashrc" ]; then
	    . "$HOME/.bashrc"
    fi
fi
. "$HOME/.cargo/env"

# opam configuration
test -r /home/aron/.opam/opam-init/init.sh && . /home/aron/.opam/opam-init/init.sh > /dev/null 2> /dev/null || true

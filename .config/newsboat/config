# Settings
max-items 1000

# Vim keybindings
bind-key g home
bind-key G end
bind-key j down
bind-key k up
bind-key l open
bind-key h quit
bind-key ^U pageup
bind-key ^D pagedown

# Formatting
feedlist-format "%4i %n %-10u %t (%T)"
articlelist-format "%4i %n %D %?T?%-20T  ?%t"
